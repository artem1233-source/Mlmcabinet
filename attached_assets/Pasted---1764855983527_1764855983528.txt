Сейчас хочу протестировать математику вознаграждений при продаже продукта, но в интерфейсе не работает сама покупка:
	•	в каталоге товаров кнопки «Купить» и «Продать гостю» визуально активны, но при клике ничего не происходит;
	•	не открывается окно оформления заказа, не уходит запрос на сервер, в UI нет ни ошибки, ни подтверждения.

Требуется:
	1.	Воспроизвести проблему
	•	Запусти приложение в Preview.
	•	Авторизуйся под обычным партнёром (не админ, если есть роль).
	•	Открой страницу каталога/продаж (компоненты вроде CatalogRu.tsx, CartRu.tsx, CheckoutRu.tsx).
	•	Нажми «Купить» и «Продать гостю».
	•	Посмотри:
	•	есть ли ошибки в браузерной консоли;
	•	появляются ли запросы в Network;
	•	есть ли ошибки в серверных логах Replit.
	2.	Найти, где привязана логика кнопок
	•	Найди компонент(ы), где отрисовываются эти кнопки:
	•	вероятно CatalogRu.tsx, CatalogNew.tsx, CheckoutRu.tsx или подобные.
	•	Найди onClick-обработчики для действий:
	•	покупка для себя (партнёра);
	•	продажа гостю.
	•	Определи:
	•	вызывается ли там функция вроде createOrder, createGuestOrder, openCheckout, handleBuy и т.п.;
	•	есть ли условия disabled, if (!something) return, canBuy, isDemoMode, isPaymentConfigured и т.д., которые блокируют клик.
	3.	Проверить связь с backend
	•	Найди на сервере эндпоинты, связанные с созданием заказов и гостевых покупок:
	•	в src/supabase/functions/server/index.tsx — роуты /orders, /create-order, /guest-order и т.п.;
	•	Убедись, что:
	•	фронтенд действительно вызывает существующий эндпоинт;
	•	URL и метод совпадают;
	•	нет блокировки по режиму (например, демо-режим, флаг отключения оплаты, обязательная интеграция с ЮKassa и т.д.).
	4.	Починить цепочку «кнопка → заказ»
	•	Сделай так, чтобы при нажатии:
	•	либо открывалось окно оформления заказа,
	•	либо сразу отправлялся запрос на сервер для создания заказа.
	•	Если какие-то проверки нужны (наличие метода оплаты, выбранный продукт, роль пользователя) — оставь их, но:
	•	добавь понятный UI-ответ (toast/alert), если покупка заблокирована: например, «Оплата временно недоступна» или «Метод оплаты не настроен».
	•	Важно:
	•	не ломай и не дублируй новую систему комиссий: комиссионная математика должна по-прежнему идти через product.commission + calcOrder и order.payouts.
	•	Никаких новых хардкодов 1600/900/500/200 в фронтенде и бэкенде.
	5.	Проверка после правок
	•	В Preview:
	•	сделай тестовую покупку на себя;
	•	сделай тестовую «продажу гостю».
	•	Убедись, что:
	•	в Network уходит запрос создания заказа;
	•	в ответе сервера нет ошибок;
	•	в UI видно, что заказ создан (либо в списке заказов, либо через уведомление).
	•	Если backend возвращает ошибку (например, из-за отсутствия настроек оплаты) — не пытайся «подменять» логику, просто:
	•	корректно отобрази сообщение пользователю;
	•	в консоли выведи подробную диагностику.
	6.	Итог
	•	Покажи diff всех изменённых файлов.
	•	Кратко опиши:
	•	что именно было сломано (например, onClick не был привязан, кнопка была всегда disabled, несуществующий эндпоинт и т.п.);
	•	как теперь работает полный путь «кнопка → запрос → ответ → UI».

Не трогай, пожалуйста, работу MLM-рангов и уже настроенную комиссионную логику (calcOrder + order.payouts) — нужна только починка самого процесса покупки и продажи гостю.