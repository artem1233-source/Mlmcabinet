# 💰 СИСТЕМА ГИБКИХ КОМИССИЙ - РУКОВОДСТВО

## ✅ Что сделано

Реализована система **настраиваемых комиссий для каждого продукта**. Теперь админ может задавать индивидуальные комиссии для каждого товара без изменения кода.

---

## 📦 Новые файлы

### 1. `/utils/types/commission.ts`
Содержит:
- **Типы данных** для комиссий (`CommissionLevel`, `ProductCommission`)
- **Дефолтные комиссии** (`DEFAULT_COMMISSIONS`)
- **Функции валидации** (`validateCommission`)
- **Функцию получения комиссий** (`getProductCommissions`)

### 2. `/components/CommissionEditor.tsx`
- Визуальный редактор комиссий
- Отдельные секции для розничных и партнёрских продаж
- Поддержка до 5 уровней (L0-L5)
- Валидация значений
- Подсказки и рекомендации

---

## 🔧 Изменённые файлы

### 1. `/utils/mlm.ts` ✅
**Что изменилось:**
- Добавлен опциональный параметр `product?: ProductWithCommission` в `calcOrder()`
- Функции `getCommissionInfo()` и `calcIncome()` теперь принимают как SKU (string), так и объект продукта
- **ВАЖНО:** Все старые сигнатуры функций сохранены для обратной совместимости

**Как работает:**
```typescript
// Старый способ (всё ещё работает)
calcOrder({ isPartner: true, sku: 'H2-1', u1, u2, u3 });

// Новый способ (с кастомными комиссиями)
calcOrder({ 
  isPartner: true, 
  sku: 'H2-1', 
  u1, u2, u3,
  product: productWithCommissions  // Опционально!
});
```

### 2. `/utils/demoData.ts` ✅
**Что изменилось:**
- Добавлены комиссии в базовые продукты `PRODUCTS`
- Функция `generateCatalogProducts()` создаёт продукты с комиссиями
- Использованы **те же значения комиссий**, что были раньше (для совместимости)

### 3. `/components/AdminRu.tsx` ✅
**Что добавлено:**
- Импорт `CommissionEditor` и типов комиссий
- Состояние `productCommission` для хранения комиссий
- Обновлены функции `handleCreateProduct()` и `handleUpdateProduct()` для сохранения комиссий
- Обновлены `resetProductForm()` и `openEditProduct()` для работы с комиссиями
- Добавлен `CommissionEditor` в форму создания/редактирования товара

---

## 🎯 Как использовать

### Для администратора:

1. **Открыть админ-панель** → вкладка "Товары"
2. **Создать новый товар** или **отредактировать существующий**
3. **Прокрутить вниз до секции "Комиссии продукта"**
4. **Настроить комиссии:**
   - **Розничные продажи (гость):** L0 - комиссия продавцу
   - **Партнёрские продажи:** L1/L2/L3 - комиссии по линиям
5. **Сохранить товар**

### Для разработчиков:

#### Получить комиссии продукта:
```typescript
import { getProductCommissions } from './utils/types/commission';

// Из объекта продукта
const commissions = getProductCommissions(product);

// По SKU (fallback на дефолтные)
const commissions = getProductCommissions('H2-1');
```

#### Рассчитать заказ с кастомными комиссиями:
```typescript
import { calcOrder } from './utils/mlm';

const result = calcOrder({
  isPartner: true,
  sku: product.sku,
  u1: sponsor.id,
  u2: sponsor2.id,
  u3: sponsor3.id,
  product: product  // ← Передаём продукт с комиссиями
});

console.log(result.price);    // Цена
console.log(result.payouts);  // Массив выплат
```

---

## 🔄 Обратная совместимость

### ✅ Все старые функции работают БЕЗ изменений!

```typescript
// Это всё ещё работает (без product parameter)
calcOrder({ isPartner: true, sku: 'H2-1', u1, u2, u3 });

// Это тоже работает (со строкой SKU)
calcIncome('H2-1');
getCommissionInfo('H2-3');
```

### Fallback логика:
1. Если передан `product` с `commission` → используются комиссии продукта
2. Если передан `product` без `commission` → fallback на `DEFAULT_COMMISSIONS[sku]`
3. Если передан только `sku` → используются `DEFAULT_COMMISSIONS[sku]`
4. Если SKU не найден → используются `DEFAULT_COMMISSIONS['H2-1']`

---

## 📊 Структура комиссий

### ProductCommission:
```typescript
{
  guest: {
    L0?: number;  // Продавец (розничная продажа)
  },
  partner: {
    L1?: number;  // 1-я линия (прямой спонсор)
    L2?: number;  // 2-я линия
    L3?: number;  // 3-я линия
    L4?: number;  // 4-я линия (опционально)
    L5?: number;  // 5-я линия (опционально)
  }
}
```

### Пример (H2-1):
```typescript
{
  guest: { L0: 1600 },
  partner: { L1: 900, L2: 500, L3: 200 }
}
```

---

## 🎨 UI Редактора комиссий

### Розничные продажи:
- Одно поле: **L0** (продавец)
- Отображение итоговой суммы

### Партнёрские продажи:
- Основные поля: **L1, L2, L3**
- Дополнительные (скрыты по умолчанию): **L4, L5**
- Отображение итоговой суммы

### Валидация:
- Все значения должны быть ≥ 0
- Хотя бы одна секция (guest или partner) должна быть заполнена
- Визуальные подсказки и рекомендации

---

## 🚀 Следующие шаги (опционально)

1. **Добавить предустановки** - шаблоны комиссий для быстрого выбора
2. **Калькулятор маржи** - проверка, что комиссии не превышают маржу
3. **История изменений** - логирование изменений комиссий
4. **Массовое редактирование** - изменение комиссий для группы товаров
5. **Экспорт/импорт** - сохранение и загрузка схем комиссий

---

## ⚠️ Важные заметки

1. **Не удалены старые константы** в `/utils/mlm.ts` - они используются как fallback
2. **Демо-данные не изменены** - используются те же значения комиссий
3. **Все существующие вызовы работают** - новый параметр `product` опциональный
4. **Базовые продукты** (H2-1, H2-2) помечены `isBaseProduct: true` и не удаляются через UI

---

## 🐛 Отладка

### Проверить комиссии продукта:
```typescript
console.log('Product commissions:', product.commission);
```

### Проверить результат расчёта:
```typescript
const result = calcOrder({ ...params, product });
console.log('Calculated payouts:', result.payouts);
```

### Проверить fallback:
```typescript
// Продукт без commission
const product = { sku: 'H2-1' };
const result = calcOrder({ ...params, product });
// Должны использоваться DEFAULT_COMMISSIONS['H2-1']
```

---

## ✅ Чеклист интеграции

- [x] Создана типизация комиссий
- [x] Обновлена логика расчёта в `mlm.ts`
- [x] Добавлены комиссии в демо-данные
- [x] Создан UI редактор комиссий
- [x] Интегрирован в админ-панель
- [x] Обратная совместимость гарантирована
- [x] Валидация данных работает
- [x] Документация создана

---

**Автор:** AI Assistant  
**Дата:** 2024  
**Версия:** 1.0
