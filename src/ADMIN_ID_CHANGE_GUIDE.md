# 📝 Руководство по смене ID администраторов

## 🎯 Назначение

Функция позволяет **CEO/главному администратору** изменять ID для себя и других администраторов с русских символов на английские (например, `СЕО` → `CEO`).

---

## ✅ Что реализовано

### 1. **Компонент AdminIdManager** (`/components/admin/AdminIdManager.tsx`)
- Отображает список всех администраторов в системе
- Позволяет изменить ID для любого администратора
- Валидирует новый ID (только латинские буквы, цифры, дефис, подчёркивание)
- Автоматически обновляет все ссылки в системе

### 2. **Интеграция в настройки** (`/components/SettingsRu.tsx`)
- Компонент отображается только для администраторов (`isAdmin === true`)
- Автоматически загружает список всех пользователей
- Обновляет данные после изменения ID

### 3. **Серверная часть** (`/supabase/functions/server/index.tsx`)
- Эндпоинт `/admin/change-user-id` уже реализован
- Обновляет ID пользователя и все ссылки на него:
  - Спонсоры (`спонсорId`)
  - Команды (`команда`)
  - Заказы (автоматически через связи)

---

## 🚀 Как использовать

### Для CEO:

1. **Войдите в систему** как администратор
2. **Откройте раздел "Настройки"** в главном меню
3. **Прокрутите до секции** "Управление ID администраторов" (красная карточка с иконкой щита)
4. **Найдите нужного администратора** в списке
5. **Нажмите "Изменить ID"**
6. **Введите новый ID** (например: `CEO`, `admin`, `director`)
7. **Подтвердите изменение**

### Правила для нового ID:

✅ **Разрешено:**
- Латинские буквы (a-z, A-Z)
- Цифры (0-9)
- Дефис (-)
- Подчёркивание (_)
- Минимум 2 символа

❌ **Запрещено:**
- Русские буквы
- Пробелы
- Спецсимволы (кроме дефиса и подчёркивания)
- ID должен быть уникальным

### Примеры корректных ID:

```
CEO
admin
director
manager_1
super-admin
support
```

---

## ⚠️ Важные замечания

### 1. Изменение собственного ID

Если вы изменяете **свой собственный ID**:
- ⚠️ Страница автоматически перезагрузится
- 🔄 Система обновит ваш токен авторизации
- ✅ Вход будет выполнен автоматически с новым ID

### 2. Изменение ID другого администратора

При изменении ID другого администратора:
- ✅ Все ссылки обновятся автоматически
- ✅ Пользователь сможет войти с новым ID
- 📧 Рекомендуется уведомить его о смене ID

### 3. Автоматическое обновление ссылок

При смене ID система автоматически обновляет:
- 👥 Всех спонсоров (поле `спонсорId`)
- 🌳 Все команды (массив `команда`)
- 📦 Все заказы и транзакции (связи через ID)

---

## 🔧 Технические детали

### API Эндпоинт

```typescript
POST /admin/change-user-id
Headers:
  X-User-Id: <current-admin-id>
  Authorization: Bearer <anon-key>

Body:
{
  "oldId": "СЕО",
  "newId": "CEO"
}

Response:
{
  "success": true,
  "message": "ID изменён: СЕО → CEO. Обновлено 15 ссылок.",
  "updatedReferences": 15,
  "log": [...]
}
```

### Валидация

**На клиенте:**
```typescript
- Минимум 2 символа
- Только: a-zA-Z0-9_-
- Не пустая строка
```

**На сервере:**
```typescript
- ID уникален
- Пользователь существует
- Администратор имеет права
```

---

## 📊 Пример использования

### Сценарий: Смена ID для CEO с русского на английский

**До:**
```json
{
  "id": "СЕО",
  "имя": "Администратор",
  "email": "admin@admin.com",
  "isAdmin": true
}
```

**Действия:**
1. CEO открывает Настройки
2. Находит свой аккаунт в списке администраторов
3. Нажимает "Изменить ID"
4. Вводит: `CEO`
5. Подтверждает

**После:**
```json
{
  "id": "CEO",
  "имя": "Администратор",
  "email": "admin@admin.com",
  "isAdmin": true
}
```

**Результат:**
- ✅ Все пользователи с `спонсорId: "СЕО"` → `спонсорId: "CEO"`
- ✅ Все массивы команд обновлены
- ✅ CEO может входить с новым ID
- ✅ Страница перезагружена автоматически

---

## 🐛 Решение проблем

### Проблема: "ID уже занят"

**Причина:** Новый ID уже используется другим пользователем

**Решение:** 
- Выберите другой ID
- Проверьте список пользователей

### Проблема: "Доступ запрещён"

**Причина:** Текущий пользователь не администратор

**Решение:**
- Убедитесь что вы вошли как администратор
- Проверьте флаг `isAdmin === true`

### Проблема: После смены ID нельзя войти

**Причина:** Токен авторизации устарел

**Решение:**
- Очистите localStorage: `localStorage.clear()`
- Перезагрузите страницу
- Войдите с новым ID

---

## 💡 Рекомендации

1. **Планируйте заранее:** Выберите постоянный ID перед началом активного использования
2. **Документируйте:** Записывайте изменения ID в журнале
3. **Уведомляйте:** Сообщайте администраторам об изменении их ID
4. **Тестируйте:** После смены проверьте что вход работает корректно
5. **Резервные копии:** Сделайте бэкап перед массовым изменением ID

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Убедитесь что вы администратор
4. Проверьте формат нового ID

---

✅ **Функция готова к использованию!**
