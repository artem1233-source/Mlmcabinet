# 📂 Список файлов для скачивания

## Встроенное админ-управление - Inline Admin Mode

---

## 🆕 Новые файлы (1 файл)

### Скачать и создать:

```
/components/AdminToolbar.tsx
```

**Описание:** Floating индикатор режима администратора (золотой badge в правом верхнем углу)

---

## 🔄 Файлы для замены (5 файлов)

### Скачать и **полностью заменить** существующие:

```
/components/DashboardRu.tsx
/components/CatalogRu.tsx
/components/BalanceRu.tsx
/utils/api.ts
/utils/demoApi.ts
```

### Что изменено:

#### `/components/DashboardRu.tsx`
- ✅ Добавлен импорт `AdminToolbar`
- ✅ Определение админа: `const isAdmin = currentUser?.email === 'admin@admin.com'`
- ✅ Загрузка глобальной статистики через `api.getAdminStats()`
- ✅ Рендер `<AdminToolbar />` для админа
- ✅ Секция с глобальной статистикой (выручка, партнёры, заказы, выплаты)
- ✅ Визуальное разделение глобальной и личной статистики

#### `/components/CatalogRu.tsx`
- ✅ Добавлены импорты: `AdminToolbar`, `Dialog`, `Input`, `Label`, `Textarea`
- ✅ Добавлены иконки: `Plus`, `Edit2`, `Trash2`, `Save`, `X`, `Upload`
- ✅ Определение админа: `const isAdmin = currentUser?.email === 'admin@admin.com'`
- ✅ Состояния для админ-панели: `showProductModal`, `editingProduct`, `productForm`
- ✅ Функции: `handleImageUpload`, `handleCreateProduct`, `handleUpdateProduct`, `handleDeleteProduct`
- ✅ Рендер `<AdminToolbar />` для админа
- ✅ Кнопка "+ Добавить товар" в шапке
- ✅ Кнопки "Редактировать" и "🗑️" на карточках товаров
- ✅ Модальное окно создания/редактирования товара

#### `/components/BalanceRu.tsx`
- ✅ Добавлены импорты: `AdminToolbar`, `Textarea`, `Badge`
- ✅ Добавлены иконки: `CheckCircle`, `XCircle`, `Edit3`
- ✅ Определение админа: `const isAdmin = currentUser?.email === 'admin@admin.com'`
- ✅ Состояния: `allWithdrawals`, `editingWithdrawal`, `withdrawalStatus`, `withdrawalNote`
- ✅ Загрузка всех выплат через `api.getAllWithdrawals()`
- ✅ Функция: `handleUpdateWithdrawalStatus`
- ✅ Рендер `<AdminToolbar />` для админа
- ✅ Секция "Все заявки на выплату" с inline редактированием

#### `/utils/api.ts`
- ✅ Добавлена функция `getAllWithdrawals()` - алиас для `getAllWithdrawalsAdmin()`
- ✅ Добавлена функция `uploadImage(file)` - алиас для `uploadProductImage()` с другим форматом ответа

#### `/utils/demoApi.ts`
- ✅ Добавлена функция `demoGetAllWithdrawals()` для поддержки демо-режима

---

## 📊 Итого

| Категория | Количество |
|-----------|------------|
| Новые файлы | 1 |
| Файлы для замены | 5 |
| **ВСЕГО** | **6 файлов** |

---

## 🚀 Быстрая команда для копирования (пример)

Если вы работаете в терминале:

```bash
# Создайте папку для скачивания
mkdir inline-admin-files

# Скопируйте файлы из Figma Make в эту папку:
# 1. AdminToolbar.tsx
# 2. DashboardRu.tsx
# 3. CatalogRu.tsx
# 4. BalanceRu.tsx
# 5. api.ts
# 6. demoApi.ts

# Затем скопируйте в ваш проект:
cp inline-admin-files/AdminToolbar.tsx ./components/
cp inline-admin-files/DashboardRu.tsx ./components/
cp inline-admin-files/CatalogRu.tsx ./components/
cp inline-admin-files/BalanceRu.tsx ./components/
cp inline-admin-files/api.ts ./utils/
cp inline-admin-files/demoApi.ts ./utils/
```

---

## 📦 Альтернатива: Экспорт через UI

В Figma Make:
1. Откройте файл
2. Нажмите на кнопку меню (три точки)
3. Выберите "Download" или "Copy content"
4. Вставьте в ваш проект

---

## ✅ После копирования файлов

Откройте `/TRANSFER_CHECKLIST.md` и следуйте пошаговой инструкции для проверки функционала.

---

## 🎯 Результат

После копирования всех файлов у вас будет:
- 👑 Золотой badge "Режим администратора" для админов
- 📊 Глобальная статистика в Dashboard
- 🛍️ Inline управление товарами в Catalog
- 💰 Управление выплатами в Balance
- 🎨 Единый интерфейс без отдельной админ-панели

---

**Общий размер:** ~45-50 КБ  
**Время на перенос:** ~5-10 минут  
**Сложность:** ⭐⭐☆☆☆ (Легко)
