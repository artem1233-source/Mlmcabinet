{
  "buildCommand": "OUTPUT_DIR=build npm run build && ls -la build/ && echo '✅ build/ folder exists'",
  "outputDirectory": "build",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(?!.*\\.html$)(.*)",
      "destination": "/index.html"
    }
  ]
}