{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "vite",
  "build": {
    "env": {
      "OUTPUT_DIR": "build"
    }
  },
  "rewrites": [
    {
      "source": "/(?!.*\\.html$)(.*)",
      "destination": "/index.html"
    }
  ]
}