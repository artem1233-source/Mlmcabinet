{
  "buildCommand": "npx vite build --outDir dist",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(?!.*\\.html$)(.*)",
      "destination": "/index.html"
    }
  ]
}