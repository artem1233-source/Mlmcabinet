# 🔐 Настройка админ-панели

## ✅ Что сделано

### 1. **Вариант 1: Вход по email с флагом `isAdmin`**

Система автоматически устанавливает флаг `isAdmin: true` для **первого зарегистрированного пользователя**.

- ✅ Проверка прав через поле `isAdmin` в базе данных
- ✅ Кнопка "Админ-панель" в sidebar появляется автоматически для админов
- ✅ Все API-роуты защищены проверкой `requireAdmin()`
- ✅ OAuth через Google полностью поддерживается

### 2. **Функционал админ-панели**

Создано **9 разделов** с полным CRUD функционалом:

#### 📊 **Блок 1: Обзор и аналитика**
- Финансовая статистика (общий доход, выплаты партнёрам, прибыль системы)
- Топ-10 партнёров по продажам
- Ключевые метрики (конверсия, средний чек)
- Распределение партнёров по уровням

#### 👥 **Блок 2: Управление пользователями**
- Список всех пользователей
- Просмотр балансов и уровней
- Отображение админ-статуса

#### 🛍️ **Блок 3: Управление товарами** (НОВЫЙ!)
- ✅ Создание товаров
- ✅ Редактирование товаров
- ✅ Удаление товаров
- ✅ Цены по уровням (цена1, цена2, цена3, розничная)
- ✅ Загрузка изображений (URL)
- ✅ Категории товаров
- ✅ Статус активности

#### 📦 **Блок 4: Управление заказами**
- Просмотр всех заказов
- Статусы заказов (paid/pending)
- Фильтрация по дате

#### 💰 **Блок 5: Управление выплатами**
- Список заявок на выплаты
- Изменение статуса выплат (pending → processing → completed/rejected)
- Добавление примечаний

#### 🎓 **Блок 6: Управление обучением** (НОВЫЙ!)
- ✅ Создание уроков
- ✅ Редактирование уроков
- ✅ Удаление уроков
- ✅ Видео (YouTube/Vimeo)
- ✅ Категории обучения
- ✅ Доступ по уровням (урок только для Уровня 2+)
- ✅ Порядок отображения

#### 🎁 **Блок 7: Промокоды** (НОВЫЙ!)
- ✅ Создание промокодов
- ✅ Редактирование промокодов
- ✅ Удаление промокодов
- ✅ Типы скидок (процент % или фиксированная сумма ₽)
- ✅ Лимит использований
- ✅ Срок действия

#### ⚙️ **Блок 8: Настройки MLM** (НОВЫЙ!)
- ✅ Минимальная сумма вывода
- ✅ Комиссии d1/d2/d3 (глобально)
- ✅ Методы оплаты

#### 📝 **Блок 9: Логи** (НОВЫЙ!)
- Логи действий админа
- История всех изменений

---

## 🚀 Как получить доступ к админ-панели

### Способ 1: Автоматически (для первого пользователя)

Если вы **первый зарегистрированный пользователь** в системе, вам автоматически присваивается флаг `isAdmin: true`.

**Ваш статус:**
- Email: `artem1233@gmail.com`
- Авторизация: Google OAuth ✅
- Если вы первый пользователь → админ-панель уже доступна!

**Проверка:**
1. Войдите в приложение через Google
2. Откройте sidebar (боковое меню)
3. Если вы админ, увидите кнопку **"Админ-панель"** 🛡️

---

### Способ 2: Вручную через API (для других пользователей)

Если нужно сделать другого пользователя админом:

```bash
# Получите userId пользователя (например: u_oauth_xxxxx)
# Вызовите API для установки флага isAdmin

curl -X POST https://vbjueuhgcyfberivihiv.supabase.co/functions/v1/make-server-05aa3c8a/admin/users/{userId}/set-admin \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer eyJhbGci..." \
  -H "X-User-Id: YOUR_ADMIN_USER_ID" \
  -d '{"isAdmin": true}'
```

**Важно:** Это может сделать только текущий админ!

---

## 📋 API Endpoints (Админ)

Все роуты защищены проверкой `requireAdmin()`:

### Товары
- `GET /admin/products` - Получить все товары
- `POST /admin/products` - Создать товар
- `PUT /admin/products/:id` - Обновить товар
- `DELETE /admin/products/:id` - Удалить товар

### Обучение
- `GET /admin/training` - Получить все уроки
- `POST /admin/training` - Создать урок
- `PUT /admin/training/:id` - Обновить урок
- `DELETE /admin/training/:id` - Удалить урок

### Промокоды
- `GET /admin/promos` - Получить все промокоды
- `POST /admin/promos` - Создать промокод
- `PUT /admin/promos/:id` - Обновить промокод
- `DELETE /admin/promos/:id` - Удалить промокод

### Настройки
- `GET /admin/settings` - Получить настройки MLM
- `PUT /admin/settings` - Обновить настройки MLM

### Аналитика и логи
- `GET /admin/analytics` - Получить аналитику
- `GET /admin/logs` - Получить логи действий

---

## 🔒 Безопасность

### Защита на уровне сервера
- ✅ Все админ-роуты проверяют флаг `isAdmin`
- ✅ Функция `requireAdmin()` бросает ошибку 403 если пользователь не админ
- ✅ Используется custom header `X-User-Id` для идентификации

### Защита на уровне UI
- ✅ Кнопка "Админ-панель" показывается только если `currentUser.isAdmin === true`
- ✅ Все API вызовы обрабатывают 403 ошибки

---

## 🎯 Следующие шаги

### Вариант 3: Система ролей (будущее расширение)

Планируется добавить:
- **Роли:** `admin`, `manager`, `partner`, `guest`
- **Права доступа** для каждой роли:
  - `admin` - полный доступ ко всем блокам
  - `manager` - только просмотр + управление выплатами
  - `partner` - обычные партнёрские функции
  - `guest` - только каталог

### Вариант 2: Отдельный логин/пароль (опционально)

Если потребуется отдельная страница входа для админов:
- Создать `/admin/login` с логином и паролем
- Отдельная таблица `admins` в KV store
- Отдельная сессия для админов

---

## ✅ Проверка работы

1. **Войдите** через Google OAuth (`artem1233@gmail.com`)
2. **Откройте** sidebar → должна появиться кнопка **"Админ-панель"** 🛡️
3. **Нажмите** на неё → откроется админ-панель со всеми 9 блоками
4. **Протестируйте:**
   - Создайте товар в разделе "Товары"
   - Создайте урок в разделе "Обучение"
   - Создайте промокод в разделе "Промокоды"
   - Посмотрите аналитику в разделе "Обзор"

---

## 🐛 Troubleshooting

### Не вижу кнопку "Админ-панель"

**Решение 1:** Проверьте что вы первый пользователь
```bash
# В консоли браузера
console.log(JSON.parse(localStorage.getItem('current_user')));
// Должно быть: { ..., isAdmin: true }
```

**Решение 2:** Установите флаг вручную через API (см. "Способ 2" выше)

### Ошибка 403 "Admin access required"

- Ваш пользователь не имеет флаг `isAdmin: true`
- Попросите другого админа добавить вас через API

### Не загружаются товары/уроки

- Проверьте что сервер запущен: `https://vbjueuhgcyfberivihiv.supabase.co/functions/v1/make-server-05aa3c8a/health`
- Проверьте консоль браузера на ошибки

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера в Supabase Dashboard
3. Посмотрите раздел "Логи" в админ-панели

---

**Создано:** 15 ноября 2024  
**Версия:** 1.0  
**Статус:** ✅ Полностью функционально
