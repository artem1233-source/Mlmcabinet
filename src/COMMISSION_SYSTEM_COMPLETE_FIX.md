# ✅ ПОЛНОЕ ИСПРАВЛЕНИЕ СИСТЕМЫ КОМИССИЙ

## Что было исправлено

### Проблема
Когда создавались новые товары с кастомными комиссиями (например, L0 = 1000₽ вместо 1600₽), эти комиссии **НЕ** использовались в расчетах доходов партнеров. Система продолжала использовать жестко зашитые константы из `DEFAULT_COMMISSIONS`.

### Причина
В функциях `demoCreateProduct` и `demoUpdateProduct` поля `commission`, `retail_price` и `partner_price` **не сохранялись** в объект товара, хотя передавались из AdminRu.

### Решение
Исправлена вся цепочка от создания товара до отображения доходов:

#### 1. ✅ demoCreateProduct (строки 497-558)
Теперь **сохраняет** в объект товара:
```javascript
commission: productData.commission || null,
retail_price: Number(productData.retail_price) || Number(productData.цена_розница) || 0,
partner_price: Number(productData.partner_price) || Number(productData.цена1) || 0,
```

#### 2. ✅ demoUpdateProduct (строки 660-741)
Теперь **сохраняет** при обновлении:
```javascript
commission: productData.commission || data.products[productIndex].commission || null,
retail_price: Number(productData.retail_price) || цена_розница,
partner_price: Number(productData.partner_price) || цена1,
```

#### 3. ✅ calcOrder (уже работало правильно)
- Принимает полный объект `product`
- Использует `product.commission` если есть
- Fallback на `DEFAULT_COMMISSIONS` только если нет своих комиссий
- Возвращает готовые `payouts`

#### 4. ✅ processOrderCommissions (уже работало правильно)
- Использует **готовые** `order.payouts` из calcOrder
- **НЕ пересчитывает** комиссии заново
- Просто начисляет указанные суммы на балансы и создает earnings

#### 5. ✅ DashboardRu / EarningsRu (уже работали правильно)
- Берут данные напрямую из `earnings`
- **НЕ пересчитывают** по sku или DEFAULT_COMMISSIONS
- Показывают реальные начисленные суммы

## Как работает полная цепочка

```
1. AdminRu → handleCreateProduct/handleUpdateProduct
   ├─ Собирает productData с commission
   └─ Отправляет в api.createProduct(productData)

2. demoCreateProduct / demoUpdateProduct
   ├─ Получает productData.commission
   ├─ Сохраняет в product.commission ✅ НОВОЕ!
   └─ Возвращает product

3. CatalogRu → "Продать гостю"
   ├─ Вызывает api.createOrder(sku, false, 1)
   └─ Передает только SKU (это нормально)

4. demoCreateOrder
   ├─ Находит product по SKU
   ├─ Передает ПОЛНЫЙ product в calcOrder ✅ УЖЕ РАБОТАЛО
   └─ Получает готовые payouts

5. calcOrder
   ├─ Получает product.commission
   ├─ Использует эти комиссии для расчета ✅ УЖЕ РАБОТАЛО
   └─ Возвращает { price, payouts }

6. processOrderCommissions
   ├─ Получает готовые order.payouts
   ├─ НЕ пересчитывает! ✅ УЖЕ РАБОТАЛО
   └─ Создает earnings с этими суммами

7. DashboardRu / EarningsRu
   ├─ Загружают earnings из API
   ├─ Показывают суммы из earnings.сумма ✅ УЖЕ РАБОТАЛО
   └─ НЕ пересчитывают по DEFAULT_COMMISSIONS
```

## Тестирование

### Шаг 1: Очистка данных
```javascript
// Выполните в консоли браузера:
localStorage.clear();
location.reload();
```

### Шаг 2: Создание нового товара с кастомными комиссиями

1. Зайдите в **Админ-панель** → **Управление товарами**
2. Нажмите **"Добавить товар"**
3. Заполните:
   - Название: `Тестовый товар (L0=1000)`
   - SKU: `TEST-1000`
   - Описание: `Товар для тестирования комиссий`
   - Розничная цена: `5000`
   - Партнёрская цена (Уровень 1): `4000`

4. В разделе **"Комиссии продукта"** установите:
   - **Розничные продажи (гость)**:
     - L0 - Продавец: `1000` (вместо стандартных 1600)
   
   - **Партнёрские продажи**:
     - L1 - 1-я линия: `500`
     - L2 - 2-я линия: `250`
     - L3 - 3-я линия: `100`

5. Нажмите **"Создать"**

### Шаг 3: Проверка сохранения

Откройте консоль браузера и выполните:
```javascript
const data = JSON.parse(localStorage.getItem('mlm_demo_data'));
const product = data.products.find(p => p.sku === 'TEST-1000');
console.log('Commission:', product.commission);
console.log('Retail price:', product.retail_price);
console.log('Partner price:', product.partner_price);
```

**Ожидаемый результат:**
```javascript
Commission: {
  guest: { L0: 1000 },
  partner: { L1: 500, L2: 250, L3: 100 }
}
Retail price: 5000
Partner price: 4000
```

### Шаг 4: Создание гостевой продажи

1. Перейдите в **Каталог**
2. Найдите **"Тестовый товар (L0=1000)"**
3. Нажмите **"Продать гостю (₽5,000)"**
4. Подтвердите заказ

### Шаг 5: Проверка в консоли

В консоли должны появиться логи:
```
🔥 ======== calcOrder CALLED ========
🔥 Input parameters: {
  isPartner: false,
  sku: 'TEST-1000',
  product: { commission: {...}, retail_price: 5000, ... }
}

💰 calcOrder - commissions loaded: {
  sku: 'TEST-1000',
  commissions: { guest: { L0: 1000 }, partner: {...} }
}

🛒 Guest purchase calculation: {
  price: 5000,
  guestPlan: { L0: 1000 },
  ...
}

✅ Added L0 payout: { userId: 'DEMO_USER', amount: 1000 }

🔥 ======== calcOrder RESULT ========
🔥 Final payouts: [
  { userId: 'DEMO_USER', level: 'L0', amount: 1000 }
]
```

### Шаг 6: Проверка доходов

1. Перейдите в **раздел "Доходы"**
2. Проверьте таблицу транзакций
3. Найдите запись с типом **"Розничная скидка"**

**Ожидаемый результат:**
- Сумма: `₽1,000` (НЕ ₽1,600!)
- Тип: "Розничная скидка за покупку: Тестовый товар (L0=1000) x1"

4. Проверьте **баланс** в Дашборде:
   - Должен увеличиться на `₽1,000`

### Шаг 7: Создание второго товара с другими комиссиями

Повторите Шаг 2, но с другими значениями:
- Название: `Тестовый товар 2 (L0=2500)`
- SKU: `TEST-2500`
- Розничная цена: `8000`
- **L0 - Продавец: `2500`** (другое значение!)

Создайте продажу и проверьте, что в доходах появилась запись с `₽2,500`.

### Шаг 8: Проверка старых товаров

1. Создайте продажу стандартного товара **H2-1**
2. В доходах должна появиться запись с `₽1,600` (стандартное значение)

**Это подтверждает**, что:
- Новые товары используют свои комиссии ✅
- Старые товары используют DEFAULT_COMMISSIONS ✅
- Оба работают одновременно корректно ✅

## Логирование

Для отладки добавлено подробное логирование:

### demoCreateProduct / demoUpdateProduct
```
💰 Product commission received: {...}
💰 Product retail_price: 5000
💰 Product partner_price: 4000
✅ New product commission saved: {...}
```

### calcOrder
```
🔥 ======== calcOrder CALLED ========
💰 calcOrder - commissions loaded: {...}
🛒 Guest purchase calculation: {...}
✅ Added L0 payout: {...}
🔥 ======== calcOrder RESULT ========
```

### processOrderCommissions
```
💰 ========================================
💰 Processing commissions for order: ...
💰 Order payouts: [...]
💵 Before payout - Админ: balance=0, available=0
💵 After payout - Админ: balance=1000, available=1000
✅ L0: 1000.00₽ → Админ Демо (DEMO_USER)
   - Earning ID: earning_...
   - Type: розничная_скидка
   - Line: 0
💰 ========================================
```

## Что НЕ было изменено

1. ✅ **calcOrder** - уже правильно работал с product.commission
2. ✅ **processOrderCommissions** - уже использовал готовые payouts
3. ✅ **DashboardRu / EarningsRu** - уже показывали данные из earnings
4. ✅ **DEFAULT_COMMISSIONS** - остались как fallback для старых товаров

## Итого

Теперь **ВСЯ МАТЕМАТИКА** идет по правильной цепочке:

```
product.commission → calcOrder → payouts → processOrderCommissions → earnings → Dashboard/Earnings UI
```

Каждый новый товар может иметь **свои уникальные комиссии**, и они будут **правильно** использоваться во всех расчетах и отображаться в личных кабинетах партнеров.
