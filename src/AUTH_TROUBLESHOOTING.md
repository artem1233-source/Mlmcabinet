# 🔐 Решение проблем с авторизацией

## Быстрая диагностика

Если у вас возникают проблемы со входом, используйте **страницу диагностики**:

```
https://ваш-домен.com/auth-diagnostic
```

Или локально:
```
http://localhost:5173/auth-diagnostic
```

Эта страница покажет:
- ✅ Существует ли пользователь в базе данных приложения (KV Store)
- ✅ Существует ли пользователь в системе авторизации (Supabase Auth)
- ✅ Подтверждён ли email
- 💡 Рекомендации по решению проблемы

---

## Типичные ошибки и решения

### ❌ Ошибка: "Неверный пароль"

**Возможные причины:**

1. **Неправильный пароль**
   - ✅ Проверьте раскладку клавиатуры (RU/EN)
   - ✅ Проверьте CapsLock
   - ✅ Используйте функцию "Забыли пароль?"

2. **Пользователь не найден в Supabase Auth**
   - ❌ Пользователь есть в KV Store, но нет в Supabase Auth
   - ✅ **Решение:** Зарегистрируйтесь заново с тем же email
   
3. **Email не подтверждён**
   - ❌ При регистрации email не был автоматически подтверждён
   - ✅ **Решение:** Проверьте почту или обратитесь к администратору

**Шаги диагностики:**

```bash
# 1. Откройте страницу диагностики
/auth-diagnostic

# 2. Введите ваш email

# 3. Нажмите "Проверить"

# 4. Следуйте рекомендациям
```

---

### ❌ Ошибка: "Email не найден"

**Причина:** Пользователь не зарегистрирован в системе

**Решение:**
1. Проверьте правильность email
2. Зарегистрируйтесь, нажав кнопку **"Регистрация"**
3. При регистрации используйте реферальный код (если есть)

---

### ❌ Ошибка: "Пользователь с таким ID не найден"

**Причина:** Вход по числовому ID, но пользователь не существует

**Решение:**
1. Используйте **Email** вместо ID для входа
2. Если забыли email, обратитесь к администратору

---

### ⚠️ Несоответствие данных

#### Сценарий 1: Есть в KV Store, нет в Supabase Auth

**Симптомы:**
- Профиль существует в базе приложения
- При попытке входа: "Неверный пароль" или "Email не найден"

**Решение:**
```bash
1. Выйдите из аккаунта (если вошли)
2. Перейдите на страницу "Регистрация"
3. Зарегистрируйтесь с тем же email, который использовали ранее
4. Система синхронизирует данные
```

#### Сценарий 2: Есть в Supabase Auth, нет в KV Store

**Симптомы:**
- Можете войти, но нет данных профиля
- Ошибки при работе с приложением

**Решение:**
```bash
1. Обратитесь к администратору
2. Администратор должен восстановить данные профиля
   или удалить запись из Supabase Auth
```

---

## Как работает авторизация

### Двойная система хранения

**1. Supabase Auth** (система авторизации)
- Хранит: email, пароль (хешированный), метаданные
- Отвечает за: проверку логина/пароля, JWT токены
- Автоподтверждение email: ✅ включено (так как нет SMTP)

**2. KV Store** (база данных приложения)
- Хранит: профиль партнёра, баланс, команду, заказы
- Отвечает за: бизнес-логику, MLM структуру, комиссии

### Процесс регистрации

```
1. Пользователь заполняет форму регистрации
   ↓
2. Создаётся запись в Supabase Auth
   - email: user@example.com
   - password: (хешированный)
   - email_confirm: true (автоподтверждение)
   ↓
3. Создаётся профиль в KV Store
   - id: "1" (автоинкремент)
   - email: user@example.com
   - supabaseId: "uuid-from-supabase"
   - upline: { u0, u1, u2, u3 }
   - баланс: 0
   ↓
4. Пользователь автоматически входит в систему
```

### Процесс входа

```
1. Пользователь вводит email/ID + пароль
   ↓
2. Сервер ищет пользователя в KV Store
   - По email: user:email:{email}
   - По ID: user:id:{id}
   ↓
3. Получает email пользователя
   ↓
4. Проверяет пароль через Supabase Auth
   - supabase.auth.signInWithPassword({ email, password })
   ↓
5. Если успешно:
   - Возвращает профиль из KV Store
   - Возвращает access_token от Supabase
   ↓
6. Фронтенд сохраняет токен и профиль
```

---

## Инструменты диагностики

### 1. Страница диагностики авторизации
```
URL: /auth-diagnostic

Проверяет:
- Наличие пользователя в KV Store
- Наличие пользователя в Supabase Auth
- Статус подтверждения email
- Рекомендации по решению

Использование:
1. Откройте /auth-diagnostic
2. Введите email
3. Нажмите "Проверить"
```

### 2. Консоль браузера
```javascript
// Проверить сохранённый токен
console.log('Access Token:', localStorage.getItem('access_token'));

// Проверить текущего пользователя
console.log('Current User:', localStorage.getItem('currentUser'));

// Очистить все данные (если нужен полный сброс)
localStorage.clear();
location.reload();
```

### 3. Логи сервера

При входе сервер выводит подробные логи:

```
✅ Успешный вход:
Login attempt for: user@example.com
Login by Email: user@example.com
✅ User logged in: Иван Петров (ID: 1)

❌ Ошибка входа:
Login attempt for: user@example.com
❌ Supabase Auth login error: Invalid login credentials
```

---

## Администраторский доступ

### Вход как администратор

**Email:** `admin@admin.com`

При входе с этим email автоматически:
- ✅ Устанавливается флаг `isAdmin: true`
- ✅ Открывается доступ к админ-панели
- ✅ Доступны функции редактирования

### Первый пользователь (ID=1)

Пользователь с ID="1" автоматически получает права админа:
```javascript
if (user.id === '1' && !user.isAdmin) {
  user.isAdmin = true;
  await kv.set(`user:id:${user.id}`, user);
}
```

### Восстановление прав админа

Если права админа потерялись:

1. **Автоматическое восстановление при входе:**
   - Войдите как admin@admin.com или ID=1
   - Флаг восстановится автоматически

2. **Массовая миграция:**
   ```bash
   POST /make-server-05aa3c8a/admin/migrate-admin-flags
   
   Восстановит isAdmin для:
   - Пользователя с ID="1"
   - Пользователя с email="admin@admin.com"
   ```

---

## Часто задаваемые вопросы

### ❓ Можно ли изменить email?

Сейчас нет функции изменения email. Если нужно:
1. Зарегистрируйте новый аккаунт с новым email
2. Обратитесь к администратору для переноса данных

### ❓ Можно ли восстановить пароль?

Да, используйте функцию **"Забыли пароль?"** на странице входа.

**Внимание:** Для работы восстановления пароля нужно настроить SMTP в Supabase.

### ❓ Что делать, если забыл и email, и пароль?

Обратитесь к администратору. Администратор может:
1. Найти ваш профиль по имени/телефону/Telegram
2. Сообщить ваш email
3. Сбросить пароль через Supabase Dashboard

### ❓ Как удалить аккаунт?

Обратитесь к администратору. Удаление включает:
1. Удаление из Supabase Auth
2. Удаление из KV Store
3. Пересчёт структуры команды спонсора

---

## Контакты поддержки

Если проблема не решена:

1. **Проверьте логи:**
   - Откройте консоль браузера (F12)
   - Посмотрите на ошибки в Network и Console

2. **Используйте диагностику:**
   - Откройте /auth-diagnostic
   - Скопируйте результаты проверки

3. **Обратитесь к администратору:**
   - Опишите проблему
   - Приложите скриншоты
   - Укажите ваш email

---

**Последнее обновление:** 23 ноября 2025  
**Статус:** ✅ Актуально
