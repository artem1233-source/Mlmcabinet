# 🎭 ПЕРЕКЛЮЧЕНИЕ ДЕМО-ПОЛЬЗОВАТЕЛЕЙ - РУКОВОДСТВО

## ✅ ЧТО СДЕЛАНО

Реализована система **переключения между демо-пользователями** в MLM-кабинете. Теперь в демо-режиме можно входить от имени партнёров 1-й, 2-й и 3-й линий для тестирования MLM-логики, комиссий и создания заказов.

---

## 📦 СОЗДАННЫЕ ФАЙЛЫ

### `/components/DemoUserSelector.tsx`
**Селектор для переключения демо-пользователей**

**Функционал:**
- Dropdown с списком всех демо-пользователей (L0-L3)
- Отображает имя, фамилию и уровень (L0/L1/L2/L3)
- При выборе пользователя вызывает `onUserChange` колбэк
- Видимый только в демо-режиме
- Жёлтая панель сверху под TopBar

**Внешний вид:**
```
┌─────────────────────────────────────────────────┐
│ 🎭 Демо-режим • Просмотр как:                  │
│ [Администратор Системы • L0 (Вы) ▼]            │
│ Переключайтесь между пользователями...         │
└─────────────────────────────────────────────────┘
```

---

## 🔧 ИЗМЕНЁННЫЕ ФАЙЛЫ

### 1. `/utils/demoApi.ts`

#### Добавлено 6 новых функций:

**`getCurrentDemoUserId(): string | null`**
- Получает ID текущего демо-пользователя из localStorage
- Ключ: `demoCurrentUserId`

**`setCurrentDemoUser(userId: string): void`**
- Устанавливает текущего демо-пользователя
- Сохраняет в localStorage
- Логирует переключение в консоль

**`getCurrentDemoUser(): any`**
- Получает объект текущего демо-пользователя
- **Логика fallback:**
  1. Если `demoCurrentUserId` не установлен → возвращает `DEMO_USER` (админ)
  2. Если пользователь с таким ID не найден → возвращает `DEMO_USER`
  3. Иначе → возвращает найденного пользователя

**`getDemoUsersList(): Array<{ id, label, level, depth }>`**
- Возвращает список всех демо-пользователей для селектора
- Фильтрует только уровни 0-3 (`глубина >= 0 && глубина <= 3`)
- Сортирует по глубине (L0 → L1 → L2 → L3)
- Форматирует метки уровней:
  - `L0 (Вы)` - главный пользователь
  - `L1 (1-я линия)` - прямые партнёры
  - `L2 (2-я линия)` - партнёры 2-го уровня
  - `L3 (3-я линия)` - партнёры 3-го уровня

#### Обновлена функция `demoGetCurrentUser()`:
**Было:**
```typescript
export async function demoGetCurrentUser() {
  await delay(300);
  const data = getDemoData();
  return {
    success: true,
    user: data.currentUser  // ← Всегда один пользователь
  };
}
```

**Стало:**
```typescript
export async function demoGetCurrentUser() {
  await delay(300);
  // 🆕 Используем текущего выбранного демо-пользователя
  const currentUser = getCurrentDemoUser();
  
  if (!currentUser) {
    return {
      success: false,
      error: 'Demo user not found'
    };
  }
  
  return {
    success: true,
    user: currentUser  // ← Динамический пользователь!
  };
}
```

---

### 2. `/AppRu.tsx`

#### Добавлен импорт:
```typescript
import { isDemoMode, getCurrentDemoUser } from './utils/demoApi';
import { DemoUserSelector } from './components/DemoUserSelector';
```

#### Обновлена логика загрузки демо-пользователя:
**Было:**
```typescript
const freshDemoData = loadDemoDataFromStorage();
if (freshDemoData?.currentUser) {
  api.setAuthToken(freshDemoData.currentUser.id);
  setCurrentUser(freshDemoData.currentUser);
  setIsAuthenticated(true);
}
```

**Стало:**
```typescript
// 🆕 Используем getCurrentDemoUser() вместо freshDemoData.currentUser
const currentDemoUser = getCurrentDemoUser();

if (currentDemoUser) {
  api.setAuthToken(currentDemoUser.id);
  setCurrentUser(currentDemoUser);
  setIsAuthenticated(true);
  console.log('🎭 Demo user loaded:', currentDemoUser.имя, currentDemoUser.фамилия);
}
```

#### Добавлена функция `handleDemoUserChange()`:
```typescript
const handleDemoUserChange = async () => {
  if (!isDemoMode()) return;
  
  console.log('🎭 Demo user changed, refreshing...');
  const currentDemoUser = getCurrentDemoUser();
  
  if (currentDemoUser) {
    api.setAuthToken(currentDemoUser.id);
    setCurrentUser(currentDemoUser);
    refreshData(); // Обновляем все данные
    toast.success(`Переключено на: ${currentDemoUser.имя} ${currentDemoUser.фамилия}`);
  }
};
```

#### Добавлен UI селектора:
```tsx
{/* 🆕 Селектор демо-пользователей (только в демо-режиме) */}
{isDemoMode() && (
  <div className="fixed top-16 lg:top-20 left-0 right-0 z-40 lg:ml-64">
    <DemoUserSelector onUserChange={handleDemoUserChange} />
  </div>
)}
```

#### Добавлен отступ для main:
```tsx
<main className={`flex-1 overflow-auto ${isDemoMode() ? 'pt-28 lg:pt-32' : 'pt-16 lg:pt-20'}`}>
```
- В демо-режиме: `pt-28` (112px отступ сверху)
- В обычном режиме: `pt-16` (64px отступ сверху)

---

## 🎯 КАК ИСПОЛЬЗОВАТЬ

### Шаг 1: Открыть демо-режим
1. Открыть приложение с `?demo=true` в URL
2. Автоматически создаются демо-данные (если их нет)
3. Автоматический вход под `DEMO_USER` (админ, L0)

### Шаг 2: Увидеть селектор
Сразу после входа вы увидите **жёлтую панель** под топ-баром:
```
┌────────────────────────────────────────────────┐
│ 🎭 Демо-режим • Просмотр как:                 │
│ [Администратор Системы • L0 (Вы) ▼]           │
│ Переключайтесь между пользователями...        │
└────────────────────────────────────────────────┘
```

### Шаг 3: Переключиться на партнёра
1. Кликнуть на dropdown
2. Выбрать партнёра из списка:
   - **Администратор Системы • L0 (Вы)** - вы (root)
   - **Иван Иванов • L1 (1-я линия)** - прямой партнёр
   - **Мария Смирнова • L2 (2-я линия)** - партнёр 2-го уровня
   - **Дмитрий Петров • L3 (3-я линия)** - партнёр 3-го уровня
   
3. После выбора:
   - Появится toast: "Переключено на: Иван Иванов"
   - Перезагрузятся все данные (refreshData)
   - Интерфейс покажет данные выбранного пользователя

### Шаг 4: Тестировать MLM-логику
Теперь вы можете:
- **Просмотреть структуру** выбранного пользователя
- **Увидеть его заказы** и доходы
- **Создать заказ от его имени**
- **Проверить комиссии** которые получат его спонсоры

---

## 🔍 ЧТО МЕНЯЕТСЯ ПРИ ПЕРЕКЛЮЧЕНИИ

### Дашборд
- Показывает **баланс выбранного пользователя**
- Показывает **его доходы** (earnings)
- Показывает **его структуру** (команду)

### Структура
- Показывает **партнёров выбранного пользователя**
- Ограничивается 3-мя уровнями вниз от него

### Заказы
- Показывает **заказы выбранного пользователя**
- При создании нового заказа покупатель = выбранный пользователь

### Доходы (Earnings)
- Показывает **доходы выбранного пользователя**
- Фильтруются по `userId === currentUser.id`

### Баланс
- Показывает **баланс выбранного пользователя**

### Каталог
- При покупке **покупатель = выбранный пользователь**
- Комиссии рассчитываются **для цепочки спонсоров выбранного пользователя**

---

## 🧪 ПРИМЕРЫ ТЕСТИРОВАНИЯ

### Тест 1: Проверка комиссий L1
1. Переключиться на **партнёра L1** (например, Иван Иванов)
2. Создать заказ на H2-1 (4900₽)
3. **Что должно произойти:**
   - Цена для партнёра: 4900₽
   - Комиссия L1 (спонсор Ивана = DEMO_USER): 900₽
   - Комиссия L2 (спонсор DEMO_USER): 500₽
   - Комиссия L3 (спонсор L2): 200₽

### Тест 2: Проверка структуры L2
1. Переключиться на **партнёра L2** (например, Мария Смирнова)
2. Открыть вкладку "Структура"
3. **Что должно показаться:**
   - Её прямые партнёры (если есть)
   - Её команда (ограничена 3 уровнями вниз)
   - НЕ показываются партнёры выше неё

### Тест 3: Создание заказа от L3
1. Переключиться на **партнёра L3**
2. Создать заказ
3. **Что должно произойти:**
   - Покупатель = L3
   - Комиссия L1 → партнёр L2 (спонсор L3)
   - Комиссия L2 → партнёр L1 (спонсор L2)
   - Комиссия L3 → DEMO_USER (спонсор L1)

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Хранение текущего пользователя
```
localStorage.setItem('demoCurrentUserId', userId)
```
- Ключ: `demoCurrentUserId`
- Значение: ID пользователя (например, `'DEMO_USER'` или `'user_1001'`)
- Сохраняется при перезагрузке страницы

### Логика fallback
Если в localStorage нет `demoCurrentUserId` или пользователь не найден:
```typescript
const mainUser = data.users.find((u: any) => u.id === 'DEMO_USER');
if (mainUser) {
  setCurrentDemoUser(mainUser.id);
  return mainUser;
}
```
Всегда возвращается `DEMO_USER` как дефолтный.

### Фильтрация пользователей для селектора
```typescript
data.users
  .filter((u: any) => u.глубина >= 0 && u.глубина <= 3) // Только L0-L3
  .map((u: any) => ({ id, label, level, depth }))
  .sort((a, b) => a.depth - b.depth); // Сортировка по глубине
```

**Почему `глубина >= 0`?**
- `глубина: -1` = спонсор главного (выше в структуре)
- `глубина: 0` = главный пользователь (DEMO_USER)
- `глубина: 1` = 1-я линия (L1)
- `глубина: 2` = 2-я линия (L2)
- `глубина: 3` = 3-я линия (L3)
- `глубина: 4` = 4-я линия (скрыта, не показывается)

---

## 🚫 ЧТО НЕ ТРОНУТО

### ✅ Реальная авторизация
- Email авторизация работает как раньше
- Google OAuth работает как раньше
- Telegram авторизация работает как раньше
- Supabase Auth не изменён

### ✅ Production режим
- В обычном режиме (без `?demo=true`) ничего не изменилось
- Селектор НЕ показывается в production
- `getCurrentUser()` использует Supabase API

### ✅ Демо-данные
- Структура демо-данных не изменена
- `generateDemoUsers()` работает как раньше
- MLM-дерево остаётся тем же

### ✅ MLM-логика
- `/utils/mlm.ts` не изменён
- `calcOrder()` работает как раньше
- `findUpline()` работает как раньше
- Комиссии рассчитываются корректно

---

## 📊 СТРУКТУРА ДЕМО-ПОЛЬЗОВАТЕЛЕЙ

```
                    Спонсор (глубина: -1)
                          │
                          ↓
                    DEMO_USER (L0) ← Главный (админ)
                          │
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                 ↓
    User_1001 (L1)   User_1002 (L1)   User_1003 (L1)  ← 1-я линия (10 партнёров)
        │                 │                 │
    ┌───┴───┐         ┌───┴───┐         ┌───┴───┐
    ↓       ↓         ↓       ↓         ↓       ↓
 User_X  User_Y    User_A  User_B    User_C  User_D    ← 2-я линия (50 партнёров)
    │                 │                 │
    ↓                 ↓                 ↓
User_AA           User_AB           User_AC            ← 3-я линия (150-250 партнёров)
```

---

## 🎨 UI ИЗМЕНЕНИЯ

### Добавлена панель селектора:
- **Позиция:** Fixed, под TopBar
- **Цвет:** Amber (жёлтый) - `bg-amber-50`, `border-amber-200`
- **Z-index:** 40 (выше контента, ниже модальных окон)
- **Отступ слева:** `lg:ml-64` (на desktop с учётом sidebar)

### Увеличен отступ main:
- **Демо-режим:** `pt-28 lg:pt-32` (112px/128px)
- **Обычный режим:** `pt-16 lg:pt-20` (64px/80px)

---

## 🐛 ОТЛАДКА

### Проверить текущего пользователя:
```javascript
localStorage.getItem('demoCurrentUserId')
```

### Сменить пользователя вручную:
```javascript
localStorage.setItem('demoCurrentUserId', 'user_1001')
location.reload()
```

### Сбросить на DEMO_USER:
```javascript
localStorage.removeItem('demoCurrentUserId')
location.reload()
```

### Проверить список пользователей:
```javascript
import { getDemoUsersList } from './utils/demoApi';
console.log(getDemoUsersList());
```

---

## ✅ ЧЕКЛИСТ ИНТЕГРАЦИИ

- [x] Создана функция `getCurrentDemoUser()`
- [x] Создана функция `setCurrentDemoUser()`
- [x] Создана функция `getDemoUsersList()`
- [x] Обновлена `demoGetCurrentUser()` для использования текущего пользователя
- [x] Создан компонент `DemoUserSelector`
- [x] Интегрирован селектор в `AppRu.tsx`
- [x] Добавлена функция `handleDemoUserChange()`
- [x] Добавлен отступ для контента в демо-режиме
- [x] Работает переключение между пользователями
- [x] Данные обновляются после переключения
- [x] Toast-уведомления работают
- [x] Реальная авторизация не затронута
- [x] Документация создана

---

## 🚀 ГОТОВО К ИСПОЛЬЗОВАНИЮ!

Теперь в демо-режиме вы можете:
1. ✅ Переключаться между любым партнёром L0-L3
2. ✅ Просматривать их данные, структуру, заказы
3. ✅ Создавать заказы от их имени
4. ✅ Проверять начисление комиссий по MLM-логике
5. ✅ Тестировать новые комиссии через `CommissionEditor`

**Автор:** AI Assistant  
**Дата:** 2024  
**Версия:** 1.0
